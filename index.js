// Buscar y guardar en una variable todos los elementos img de mi web. De esta forma, busca todos los elementos de la web que tengan la caracteristica entre '', en este caso img

cont myImgs = document.querySelectorAll('img')

console.log(myImgs)

// Recorrer todos los elements img encontrados y cambiarles el src

myImgs.array.forEach(imgNode => {
    
});